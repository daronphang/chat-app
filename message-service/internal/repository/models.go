package repository

// How data is stored and read can be different and hence,
// requires different schemas for storing.
// Nonetheless, since current schema for storing and reading
// is the same, will use models defined in domain.